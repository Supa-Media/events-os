{
  "name": "@events-os/router",
  "version": "1.0.0",
  "private": true,
  "description": "pw-router: the Cloudflare Worker fronting publicworship.life (landing, Convex public pages, and the Expo web app).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260719.1",
    "typescript": "~5.9.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.112.0"
  }
}
