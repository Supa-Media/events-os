/**
 * STUB — implemented by this feature's builder workstream. See `./types.ts`
 * for the contract and the ONE-`.paginate()`-per-page rule.
 */
import type { ExportBuilder } from "./types";

export const givingBuilders: ExportBuilder[] = [];
