import { MarketingBlogView } from "../../../components/marketing/BlogView";

/** BLOG tab — every post, grouped by status, and the way into the editor.
 *  Body (and gate) in `components/marketing/BlogView.tsx`; see `index.tsx`
 *  for why the gate lives there. */
export default function MarketingBlogScreen() {
  return <MarketingBlogView />;
}
