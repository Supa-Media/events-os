import { MarketingEmailsSignpost } from "../../../components/marketing/EmailsSignpost";

/** EMAILS tab — a signpost to Mailchimp, not a desk. See the component for why
 *  an inert tab beats a missing one. */
export default function MarketingEmailsScreen() {
  return <MarketingEmailsSignpost />;
}
