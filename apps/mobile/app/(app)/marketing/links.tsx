import { MarketingLinksView } from "../../../components/marketing/LinksView";

/** LINKS tab — the Important Links grid, including the row that decides where
 *  the live event cards land and which ones show. Body (and gate) in
 *  `components/marketing/LinksView.tsx`; see `index.tsx` for why the gate
 *  lives there. */
export default function MarketingLinksScreen() {
  return <MarketingLinksView />;
}
