name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  ci:
    uses: Supa-Media/supa-framework/.github/workflows/ci.yml@main
    with:
      app-slug: "events-os"
      strictness: "standard"
      shared-package: "@events-os/shared"
    secrets: inherit
